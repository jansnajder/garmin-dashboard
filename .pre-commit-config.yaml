repos:
  - repo: local
    hooks:
      - id: ruff
        name: ruff
        entry: uv run --directory garmin-backend ruff check --fix .
        language: system
        types: [python]
        pass_filenames: false
